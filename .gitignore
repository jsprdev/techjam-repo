.DS_Store

# Frozen competition data. Fetched from the participant kit release, never committed.
data/

# Derived tables, eval results, sweep output and caches. These are
# regenerated by evaluation/ and are not part of the submission.
artifacts/
# EXCEPT the semantic prior, which IS part of the submission: the agent reads
# it at run time and the LLM stage contributes nothing without it. It was
# silently excluded by the rule above until a clean-clone check caught it.
!artifacts/semantic_prior.json

__pycache__/
*.pyc
.pytest_cache/
.venv/
results.json
