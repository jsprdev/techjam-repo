# Runtime dependencies. Deliberately small: the organiser may score us under
# CPU, memory and network restrictions, so nothing here downloads anything at
# import or at run time.
#
# Pinned exactly, because the score depends on the version. scikit-learn's
# TF-IDF breaks ties differently between releases, and the ranker inherits that
# ordering: the same commit scores 0.892323 on scikit-learn 1.9.0 and 0.893583
# on an older one. A floor like >=1.4 therefore does not reproduce a published
# number, which the reproducibility rules require. Tested on Python 3.12.13.
numpy==2.5.2
scikit-learn==1.9.0

# Development only. Not needed to run the agent in the official harness, and it
# cannot affect the score, so this one stays a floor rather than a pin.
pytest>=8.0
